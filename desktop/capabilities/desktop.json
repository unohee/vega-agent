{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "데스크탑 전용 권한 — 전역 단축키 + 자동 업데이트 (모바일엔 둘 다 없음)",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main", "settings"],
  "permissions": [
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "updater:default"
  ]
}
