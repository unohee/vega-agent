{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "데스크탑 전용 권한 — 자동 업데이트 (모바일엔 없음)",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main", "settings"],
  "permissions": [
    "updater:default"
  ]
}
