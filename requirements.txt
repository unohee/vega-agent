# VEGA 백엔드 런타임 의존성
# python-build-standalone 번들 Python에 pip install 할 패키지 목록

# 웹 프레임워크
fastapi==0.135.1
uvicorn==0.42.0
starlette==0.52.1
sse-starlette==3.3.4
python-multipart==0.0.22

# LLM SDK
anthropic==0.89.0
openai==2.30.0

# MCP 클라이언트 (외부 도구 서버 통합 — pipeline/mcp_client.py)
fastmcp==3.2.0
mcp==1.26.0
# typer: mcp[cli] 선택 의존 — PyInstaller 가 mcp 서브모듈 수집 시 필요
typer>=0.12

# HTTP 클라이언트
httpx==0.28.1
httpx-sse==0.4.3
aiohttp==3.13.3
requests==2.32.5

# 비동기 DB / 파일
aiosqlite==0.22.1
aiofiles==24.1.0

# 데이터 검증
pydantic==2.12.5

# 파일 변환 / 사무 문서 (VEGA.app 단독 동봉 — Docker 없이 PDF/XLS/DOCX/PPTX 처리)
mammoth==1.12.0
# 마크다운 → HTML (Superthread 카드 content 변환 등 — INT-1571)
markdown==3.10.2
openpyxl==3.1.5
pandas==2.3.3
pillow==12.2.0
pypdf==6.13.2
python-docx==1.2.0
python-pptx==1.0.2
msoffcrypto-tool==6.0.0
xlrd==2.0.2
numpy==2.4.5
matplotlib==3.10.8
plotly==6.6.0

# 보안
keyring==25.7.0
cryptography==46.0.6

# 토큰 계산
tiktoken==0.12.0

# 브라우저 자동화 (web_fetch)
playwright==1.58.0

# 시스템 유틸
psutil==7.2.2
# cron 작업 스케줄 검증/다음 실행 계산 (pipeline/cron_jobs.py) — 빠지면 cron 생성이 전부 거부됨
croniter==6.0.0
tenacity==9.1.4
rich==13.9.4
nltk==3.9.4

# 채널 어댑터 (텔레그램 / 슬랙 봇)
python-telegram-bot>=22
slack_bolt>=1.21

# 워크스페이스 통합 (Airtable 네이티브 도구 — INT-1498/1570)
pyairtable==3.3.0
